machine:
  services:
    - docker

dependencies:
  override:
    - docker info
    - ? |
        set -e
        case $CIRCLE_NODE_INDEX in
          0)
          cd nginx && docker build -t base:nginx ./
          ;;
          1)
          cd shadowsocks && docker build -t base:shadowsocks .
          ;;
          2)
          cd ipython && docker build -t base:ipython .
          ;;
        esac
      :
        parallel: true

test:
  override:
    - docker run -d base:nginx
    - docker run --restart=always -p 11111:8388 -p 11111:8388/udp -e "METHOD=chacha20" -e "PASSWORD=Arizona" -d ahxxm/base:shadowsocks
    - docker images -a --digests
    - docker history ahxxm/base:nginx
    - docker history ahxxm/base:shadowsocks
    - docker history ahxxm/base:ipython
    - docker ps
